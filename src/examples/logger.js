import Logger from '../dream_Engine/Logger'

const logger = new Logger()

logger.info('Initializing the engine')
logger.error('I got an error when player created')
logger.debug('The object is outside the screen')